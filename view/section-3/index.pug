include ./card.pug

div(class='flex gap-x-32 lg:flex-row flex-col justify-between')
 +card('title', 'GDB, the GNU Project debugger, allows you to see what is going on `inside` another program', '/fox.svg')
 +card('title', 'GDB, the GNU Project debugger, allows you to see what is going on `inside` another program', '/giraffe.svg')
 +card('title', 'GDB, the GNU Project debugger, allows you to see what is going on `inside` another program', '/penguin.svg')
